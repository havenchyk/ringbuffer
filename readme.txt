This is a naive implementation of Ring Buffer algorithm.

Source code can be found at `index.ts` file and tests that are written with help of avajs are in `test.ts`.

Setup:
1) clone the repo
2) run `npm i`
3) run `npm t`